# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /api/v1/units/si                   controllers.UnitConversionController.index(units: String ?= null)
